# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: 'Windows-latest'

steps:
- task: gitsync@1
  inputs:
    GitRepoUrl: 'https://87259a95929fa591994981a1787d37f600710741@github.com/wdeve/WVD'
